{% extends 'base.html.twig' %}

{% block title %}New Cap{% endblock %}

{% block body %}
    {% form_theme form 'bootstrap_4_layout.html.twig' %}
    <h2 class="cap d-flex justify-content-center">Crée ton défi !</h2>
    <div class="d-flex justify-content-center">
<div class="color-white">
    <div class="cast-bloc pt-2 pb-2">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    </div>
</div>
    </div>
    <div class="d-flex justify-content-center">
        <button class="btn-simple mt-4 mb-4">Valider</button>
    </div>
    {{ form_end(form) }}
{% endblock %}
